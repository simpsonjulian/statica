# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gemspec

gem 'rgl', '~> 0.6'
gem 'rubyzip', '~> 2.3'
gem 'sarif-ruby', '~> 0.1.0'

group :development do
  gem "rspec", "~> 3.13"
  gem 'rubocop'
end
